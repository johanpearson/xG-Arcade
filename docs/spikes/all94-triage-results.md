# LLM Triage — All 94 Surviving Mutants (XGArcade.Games.XGGrid)

Model: claude-opus-5. Total cost: $8.51. n=94, errors=0.

Distribution: real_gap=29, equivalent=8, noise=57, error=0

| Mutant ID | Class | Severity | File:Line | Mutator | Rationale |
|---|---|---|---|---|---|
| 11 | real_gap | high | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:71 | Logical mutation | The three covering tests only exercise the throw paths and a guess that matches nothing in PlayerNameIndex (which returns false in both variants); none asserts the early-return for an already-correct guess or an ambiguous multi-candidate result, so the mutant's fall-through into the REQ-211 live-lookup path (burning Wikidata/API-Football quota on correct guesses and potentially discarding DisambiguationCandidates) goes unnoticed. |
| 61 | real_gap | high | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:145 | Arithmetic mutation | Loosening the same-type feasibility check from `size * 2` to `size / 2` would let Trophy x Trophy be selected with only 3 seeded trophies at GridSize 3, violating REQ-102's distinct-values-per-axis rule and producing invalid/duplicate-axis grids in production; no test exercises SelectPairing's Trophy x Trophy feasibility boundary (no covering test was found at all), so the mutant survives. |
| 59 | real_gap | high | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:145 | Equality mutation | Inverting the Trophy x Trophy feasibility check makes that pairing selectable exactly when the trophy pool is too small (e.g. the 3 seeded trophies with GridSize 3), which would produce a grid violating REQ-102's distinct-values-per-axis rule or fail generation; no test asserts that Trophy x Trophy is excluded when trophyCount < size*2, and no covering test for SelectPairing was found at all. |
| 239 | real_gap | high | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:79 | Statement mutation | Dropping the `continue` makes the row-category check inert: a candidate that fails the row category but satisfies the column category would still be added to `matching` and accepted as a correct answer, breaking REQ-203's both-axes rule; no test exercises FilterByCategoriesAsync with a candidate that satisfies only the column category, so the mutant survives. |
| 422 | real_gap | high | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:268 | Boolean mutation | Initializing hadTechnicalFailure to true makes every live-queried club×club pair record a technical failure and never call ClearTechnicalFailureAsync, so pairs would be permanently skipped as 'persistent failures' after the threshold — yet no test exercises the club×club branch's success path (no covering test matched), so the wrong repository calls go unasserted. |
| 434 | real_gap | high | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:284 | Statement mutation | Deleting the ClearTechnicalFailureAsync call in the Club x Club branch means consecutive-failure counters are never reset after a successful lookup, so transient failures accumulate and the pair is permanently skipped via IsPersistentTechnicalFailureAsync (REQ-110) — and no test exercises the club-club success path's counter-reset side effect (no covering test matched this method at all), so the behavior change is invisible to the suite. |
| 437 | real_gap | high | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:286 | Negate expression | Inverting this condition makes the club×club warming loop record a 'confirmed low' data-quality marker for pairs that actually have enough valid answers (and skip recording for genuinely low ones), permanently poisoning the skip logic at lines 247-253 on later runs; no test exercises this club×club branch (no covering test matched), so the RecordConfirmedLowAsync call argument/condition is never asserted. |
| 13 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:71 | Equality mutation | The mutation makes the second disjunct (`Count < 0`) unsatisfiable, so the guard collapses to `if (result.IsCorrect)` and an ambiguous match with disambiguation candidates no longer short-circuits — it falls through into the REQ-211 live-lookup/retry path, wasting external quota and potentially replacing the disambiguation prompt. The shown tests only cover unknown instance/cell exceptions and the not-in-PlayerNameIndex gate; none exercises a match returning DisambiguationCandidates and asserts no live lookup occurs for it. |
| 15 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:71 | Null coalescing mutation (remove left) | Replacing `(result.DisambiguationCandidates?.Count ?? 0)` with `0` collapses the guard to just `result.IsCorrect`, so an ambiguous (disambiguation-candidates) result no longer short-circuits and instead falls through into the REQ-211 live-lookup/retry path — burning an external Wikidata/API-Football call and potentially changing the returned result. The three covering tests only exercise unknown-instance, unknown-cell, and a name that matches nothing in PlayerNameIndex; none produces a result with DisambiguationCandidates, so the ambiguous branch is never observed. |
| 32 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:84 | Conditional (false) mutation | Forcing the ternary false removes the REQ-102 de-duplication filter, so in a same-type pairing (e.g. Club x Club) a column header could be identical to a row header, producing a degenerate grid; the covering tests only seed a Country x Club pairing (one country + clubs, size 1), so the same-type branch is never exercised and nothing asserts row/column name distinctness. |
| 190 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:118 | Linq method mutation (FirstOrDefault() to First()) | The REQ-109 comment explicitly requires fail-closed (null) rather than throwing for an unrecognized/legacy trophy value, but no test exercises ResolveCandidateAsync with a Trophy category value absent from GetTrophiesAsync, so the mutant's InvalidOperationException (which would surface as a 500 during guess submission) goes undetected. |
| 157 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:358 | Statement mutation | Removing the shuffle makes Shuffle<T> a plain copy, so grid header selection becomes deterministic and every generated grid would reuse the same category ordering — a real product defect (repetitive grids); no test exercises randomization (no covering test matched, and Random.Shared is not injected behind a seam, so no statistical or seeded assertion exists to detect the loss of ordering variation). |
| 193 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:123 | Conditional (false) mutation | Forcing the null check false makes an unknown/legacy trophy value throw a NullReferenceException instead of returning null, directly violating the documented REQ-109 fail-closed contract; no test exercises ResolveCandidateAsync with a trophy category value absent from GetTrophiesAsync, so the defensive branch is entirely uncovered. |
| 277 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:168 | Logical mutation | The mutation inverts the exclusion filter so disambiguation candidates would list exactly the redundant row-category attributes (and drop the column exclusion entirely), directly violating REQ-209's 'other known attributes' contract; no test asserts on the DisambiguationCandidate.DistinguishingValues contents, so the wrong list ships silently. |
| 304 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:244 | Equality mutation | The mutant shifts the tier boundary so an 8-character normalized name gets tolerance 2 instead of 1, meaning REQ-208's documented rejection of near-collisions (e.g. an 8-letter name two edits from a different real player) would silently start matching; no test exercises MaxEditDistance at exactly length 8, so the boundary change goes unnoticed. |
| 296 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:210 | Linq method mutation (Any() to All()) | Any→All only differs when a candidate has multiple aliases (only one of which is within the edit distance) or an empty alias list, and no test was found exercising FindFuzzyCandidatesAsync's alias path at all — let alone with a multi-alias player, so the REQ-208 fuzzy alias matching would silently stop matching typos of one of several aliases. |
| 293 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:206 | Statement mutation | Dropping the `continue` lets a candidate whose primary name matched also fall through to the alias check and be added twice, producing duplicate Player entries that downstream ambiguity/uniqueness handling could misread (e.g. treating a single correct player as an ambiguous multi-match); no test covers a fuzzy candidate whose primary name AND an alias both fall within the edit-distance tolerance, and no test file was found exercising FindFuzzyCandidatesAsync at all. |
| 379 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:218 | Equality mutation | At exactly matches.Count == options.MinValidAnswers the mutant persists a confirmed-low marker, permanently excluding a genuinely valid pair from future cache-warming runs and grid generation (REQ-110); no test exercises the boundary case of a pair landing exactly on MinValidAnswers, and no test for this warming path was found at all. |
| 404 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:241 | Equality mutation | No test exercises the club×club warming loop at the exact cachedCount == MinValidAnswers boundary, so the off-by-one (treating an exactly-sufficient pair as invalid) survives — it would trigger needless live Wikidata queries and mis-classify pairs against REQ-110's threshold semantics without any assertion on pairsAlreadyValid or query counts catching it. |
| 440 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:288 | Statement mutation | Removing RecordConfirmedLowAsync on the Club x Club branch means low-answer pairs are never persisted as confirmed-low, so subsequent cache-warming runs re-query them live forever (defeating the REQ-110 skip logic at lines 247-253); no test exercises this branch — the heuristic found no covering test, and any existing coverage targets the analogous Country x Club loop, so the club-club duplication is untested. |
| 436 | real_gap | medium | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:286 | Equality mutation | At the exact boundary (matches.Count == MinValidAnswers) the mutant would wrongly call RecordConfirmedLowAsync, permanently marking a genuinely valid club×club pair as below-threshold so it is skipped on all future warming runs; no test exercises this club-club branch at all (no covering tests matched), let alone at the MinValidAnswers boundary. |
| 31 | real_gap | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:84 | Conditional (true) mutation | The mutant always applies the row-name exclusion filter, so it only diverges when the two axes are different category types and a country and club share a name (e.g. "Monaco"), in which case that club is wrongly dropped from the column pool; all covering tests seed disjoint names ("France" plus "*Club*") and mostly size-1 single-type grids, so no test ever exercises a cross-type name collision to detect the over-filtering. |
| 60 | real_gap | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:145 | Equality mutation | Flipping >= to > silently drops Trophy x Trophy from the candidate set at the exact boundary trophyCount == size*2 (e.g. 6 trophies, size 3), and no located test pins that boundary — existing tests apparently use trophy pools either well above or well below it, so the grid would just never produce a valid Trophy x Trophy pairing without failing anything. Impact is limited because the pairing is still unreachable with the three seeded trophies in production. |
| 270 | real_gap | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:147 | Linq method mutation (OrderBy() to OrderByDescending()) | No test asserts the ordering of DisambiguationCandidate results, so reversing OrderBy(p => p.Id) to descending survives; the documented REQ-209 contract of Id-ascending ordering (matching REQ-204's tie-break precedent) would silently flip in the API response, though both orders remain deterministic so the user-visible impact is cosmetic. |
| 307 | real_gap | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:277 | Null coalescing mutation (left to right) | The mutant flips the documented exact-name-before-alias precedence, which only diverges when a normalized name is simultaneously one player's FullName and a different player's alias; the three covering tests each seed only one of those paths (exact-only, alias-only, or no match), so no test can distinguish the ordering. Impact is limited to which canonical name/photo is shown for an already-wrong guess (cosmetic REQ-216 reveal), hence low severity. |
| 345 | real_gap | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:156 | PostIncrementExpression to PostDecrementExpression mutation | No existing test asserts on the pairsProcessed counter (tests only check TotalPairs, PairsQueriedLive, PairsAlreadyValid, PairsSkippedConfirmedLow), so inverting its increment would silently produce a negative/wrong processed count in the CacheWarmingResult and progress logging; impact is limited to reporting, not warming behavior. |
| 344 | real_gap | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:156 | Statement mutation | The existing REQ-110 tests only assert TotalPairs (computed independently from the country/club counts), PairsQueriedLive, PairsAlreadyValid and PairsSkippedConfirmedLow — none of them assert the PairsProcessed counter, so dropping the increment silently under-reports every Country x Club pair in CacheWarmingResult with no test failing. Impact is limited to operational reporting/logging rather than warming behavior. |
| 343 | real_gap | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:155 | Statement mutation | No test in the covering set ever passes an already-cancelled or mid-run-cancelled CancellationToken to WarmAsync, so removing the per-pair cooperative cancellation check goes unnoticed — a long warming run over thousands of pairs would no longer abort promptly on shutdown (it would only stop if a downstream repository/Wikidata call happens to observe the token). Severity is limited because the awaited calls inside the loop still receive the token. |
| 430 | real_gap | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:276 | Statement mutation | No test exercises the club x club cache-warming loop's technical-failure branch, so nothing asserts that the failing pair name is accumulated into failingPairs; if that list feeds the run's summary/diagnostic report, the report would silently lose the failing pair names while the separate pairsWithTechnicalFailure counter still increments, masking the loss. |
| 225 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:212 | Negate expression | Both the if-body and the fall-through path return null identically, so negating the condition cannot change observable behavior — no test can kill it regardless of coverage. |
| 224 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:212 | Logical mutation | The guarded block only does `return null`, which is identical to the fall-through `return null` on line 225, so widening `&&` to `\|\|` cannot change observable behavior for any input — no test could kill it. |
| 226 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:212 | Equality mutation | The guarded block does nothing but `return null`, which is exactly what the fall-through on line 225 does, so any change to this condition — including inverting it to `rowCategoryType != Trophy` — produces identical observable behavior and is unkillable by any test. |
| 227 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:212 | Equality mutation | The guarded block only does `return null`, which is identical to the fall-through `return null` at the end of the method, so flipping the col-type comparison cannot change observable behavior for any input — no test could kill it. |
| 228 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridLiveLookupDispatcher.cs:213 | Block removal mutation | The removed block body is just `return null;`, and the immediately following statement is also `return null;`, so removing it changes nothing observable — no test can distinguish the two paths. |
| 250 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:112 | Object initializer mutation | `IsCorrect` is a bool whose default value is already `false`, so omitting the explicit `IsCorrect = false` in the initializer produces an identical ScoreResult; no test can distinguish the two (unless ScoreResult declared a non-default initializer, which it does not). |
| 256 | equivalent | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:117 | Object initializer mutation | Removing the initializer leaves ScoreResult.IsCorrect at its default value of false for a bool, so `new ScoreResult {}` is behaviorally identical to `new ScoreResult { IsCorrect = false }`; no test can distinguish them. |
| 390 | equivalent | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:232 | Equality mutation | With i == clubs.Count the inner loop starts at j = clubs.Count + 1, which immediately fails j < clubs.Count, so the body (and the only clubs[i] access) never runs — the extra outer iteration is a no-op with no observable side effect, making the mutant unkillable regardless of test coverage. |
| 10 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:58 | String mutation | The mutation only blanks the exception's diagnostic message string; the exception type and control flow are unchanged, so the covering test (ScoreSubmissionAsync_UnknownCellId_ThrowsGuessScoringException) still passes and no user-facing behavior differs beyond log/diagnostic text. |
| 7 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:55 | String mutation | The mutation only blanks the exception's diagnostic message; the existing test asserts only that GuessScoringException is thrown, and message text is not part of any REQ-specified behavior, so this is a low-value string-mutator artifact rather than a testable gap. |
| 20 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:113 | String mutation | The mutation only blanks the diagnostic message of an exception whose type and throw-condition are already asserted by GetCellIdsAsync_UnknownInstanceId_ThrowsGuessScoringException; no caller or requirement depends on the message text, so asserting on it would be low-value string-matching in tests. |
| 22 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGameModule.cs:143 | String mutation | The mutation only blanks the exception's diagnostic message; the throw and exception type are unchanged, and REQ215_..._ThrowsGuessScoringException asserts only the type, so nothing user-visible or behavioral is lost — this is a low-value string-mutator artifact. |
| 66 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:153 | String mutation | The mutation only blanks part of an exception's diagnostic message string; the exception type (GridGenerationException) and control flow are unchanged, so no behavioral requirement is affected — tests asserting on exception type would still pass and asserting on exact message text would be brittle low-value coupling. |
| 75 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:223 | Statement mutation | The mutation removes a purely diagnostic LogInformation call with no behavioral effect on grid generation; tests assert on generated grids/exceptions, not log output, and asserting on it would add no value. |
| 67 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:154 | String mutation | The mutation only blanks part of an exception's diagnostic message text; the exception type and throw behavior are unchanged, so no meaningful behavior is lost and tests asserting on GridGenerationException would still pass (asserting exact message wording would be brittle, low-value coupling). |
| 96 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:238 | Statement mutation | The mutation removes only a LogDebug diagnostic call; the surrounding `continue` and rejection logic is unaffected, so no observable behavior changes and no reasonable test would assert on debug log output. |
| 97 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:238 | String mutation | The mutation only blanks a LogDebug message template; it has no behavioral effect on grid generation, and no reasonable test would (or should) assert on debug log text. |
| 99 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:243 | Statement mutation | The mutation removes a LogDebug call with no behavioral effect on grid generation; logging output is not asserted by any test and killing it would require brittle logger-verification tests of no product value. |
| 100 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:243 | String mutation | The mutation only blanks a LogDebug message template used for diagnostics; it has no effect on grid generation behavior, and asserting on debug log text is not a meaningful test target. |
| 119 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:266 | Statement mutation | The mutation removes only a LogWarning call; the subsequent GridGenerationException with its message is still thrown, so observable behavior is unchanged apart from log output that no test asserts on. |
| 121 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:268 | String mutation | The mutation only truncates a LogWarning message template string; the thrown GridGenerationException message and control flow are unchanged, so no reasonable test asserts on it (and none cover this method anyway). |
| 120 | noise | low | backend/src/XGArcade.Games.XGGrid/GridGenerationService.cs:267 | String mutation | The mutation only empties part of a LogWarning message template; the thrown GridGenerationException message is unaffected, so no observable behavior changes for callers and asserting on log text would be low-value. |
| 263 | noise | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:123 | String mutation | The mutation only blanks part of a LogInformation message template; log text is not asserted by any test and has no behavioral effect on the returned ScoreResult or disambiguation candidates. |
| 264 | noise | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:124 | String mutation | The mutation only alters a log message string in a LogInformation call; it has no effect on the returned ScoreResult or disambiguation candidates, so no reasonable test would (or should) assert on this literal. |
| 322 | noise | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:316 | Statement mutation | The mutation only removes a LogInformation call inside a catch block; the observable behavior (returning null so the caller falls back to PrimaryName with no photo) is unchanged, so no reasonable test could kill it short of asserting on log output. |
| 323 | noise | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:317 | String mutation | The mutation only blanks part of a LogInformation message string in a catch block; log text is not asserted by any test and has no behavioral effect (the method still returns null and the caller still falls back to PrimaryName). |
| 324 | noise | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:318 | String mutation | The mutation only alters a fragment of a log message string in an informational LogInformation call; it has no effect on control flow or returned values, and asserting on log text is not a meaningful test target. |
| 325 | noise | low | backend/src/XGArcade.Games.XGGrid/GridNameMatcher.cs:319 | String mutation | The mutation only empties a fragment of a log message string in an informational LogInformation call; it has no effect on returned values or control flow, and asserting on log text is not a meaningful test target. |
| 336 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:138 | Statement mutation | The mutation only removes an informational startup log statement; it has no effect on the returned CacheWarmingResult or any repository/Wikidata interaction, so no reasonable test would assert on it — this is a classic logging mutation artifact. |
| 337 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:139 | String mutation | The mutation only blanks part of an informational LogInformation message template; no behavior or result value changes, and asserting on log text would add no requirement-level value. |
| 338 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:140 | String mutation | The mutation only blanks part of a LogInformation message template; log text is not asserted by any test (tests assert CacheWarmingResult counts and Wikidata call counts), and structured logging tolerates extra arguments without throwing, so there is no observable behavior change worth testing. |
| 356 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:175 | Statement mutation | The mutation removes a LogDebug diagnostic call that has no behavioral effect on counters, control flow, or persisted state; tests would only kill it by asserting on debug log output, which is not a meaningful correctness requirement. |
| 357 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:175 | String mutation | The mutation only blanks a LogDebug message string; behavior, counters, and control flow are unchanged, so no reasonable test would (or should) assert on debug log text. |
| 362 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:188 | Statement mutation | The mutation removes only a LogDebug call; no observable behavior changes (counters and control flow are untouched), so no test could reasonably kill it short of asserting on debug log output. |
| 363 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:188 | String mutation | The mutation only blanks a LogDebug message template string; it has no behavioral effect on counters or control flow, and asserting on debug log text is not a meaningful test target. |
| 388 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:228 | Statement mutation | The removed statement is a pure logging progress-checkpoint helper with no effect on cache-warming behavior or persisted state, so no test could meaningfully assert it beyond log-output inspection. |
| 402 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:237 | PostIncrementExpression to PostDecrementExpression mutation | `pairsProcessed` is a progress counter consumed only by LogProgressCheckpoint's diagnostic output, not by any control flow or persisted state, so flipping the increment changes log text only — no test asserts on cache-warming progress log counts, and asserting on them would be low-value. |
| 413 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:251 | Statement mutation | The mutation removes only a LogDebug diagnostic call with no side effects on control flow, counters, or persisted state; tests assert on repository interactions and skip counters, not debug log output, so this is a low-value logging mutation artifact rather than a genuine coverage gap. |
| 401 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:237 | Statement mutation | pairsProcessed is a progress counter consumed only by LogProgressCheckpoint (and any end-of-run summary log), so dropping the increment changes only log output, not cache-warming behavior or persisted data; no test asserts on log text, and asserting on it would be low-value. |
| 400 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:236 | Statement mutation | Removing the loop's ThrowIfCancellationRequested only delays cancellation by one iteration, since every awaited repository/lookup call in the loop body already receives and honors the same cancellationToken — no test asserts on per-iteration cancellation timing in this background warming service, and doing so would be a low-value timing assertion. |
| 414 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:251 | String mutation | The mutation only blanks a LogDebug message template used for diagnostics; no observable behavior (counters, repository calls, or control flow) changes, so no test could reasonably assert on it and it carries no requirement risk. |
| 419 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:263 | Statement mutation | The mutation removes a LogDebug call with no behavioral effect on counters, persistence, or control flow; tests would only kill it by asserting on debug log output, which is not a meaningful test target. |
| 420 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:263 | String mutation | The mutation only blanks a LogDebug message template; no behavior, counter, or return value changes, so no test could meaningfully assert on it (and none inspect log output here). |
| 428 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:275 | Statement mutation | `pairsWithTechnicalFailure` is a diagnostic tally that only feeds the run's summary log line — the actual behavior (recording the failure via `RecordTechnicalFailureAsync` and adding to `failingPairs`) is untouched, so no test could meaningfully assert on it; no covering test exists for the club×club warming loop anyway. |
| 429 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:275 | PostIncrementExpression to PostDecrementExpression mutation | pairsWithTechnicalFailure is a diagnostic tally that only feeds the run's summary log/telemetry — the actual behavior (failingPairs list, RecordTechnicalFailureAsync persistence) is unaffected, so no test asserts on it and its value carries no functional consequence. |
| 431 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:276 | String mutation | The mutated string only populates a diagnostic `failingPairs` list used for summary logging, not for control flow or persisted state (the actual failure recording goes through RecordTechnicalFailureAsync with the real club names); no test asserts on log message content, and asserting on it would be low-value. |
| 445 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:296 | Statement mutation | The removed statement is a purely observational progress-logging checkpoint with no effect on the CacheWarmingResult or any persisted state, so no test could meaningfully assert on it without asserting on log output. |
| 446 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:312 | Statement mutation | The mutant removes a single Information-level summary log call that has no effect on the returned CacheWarmingResult or any observable behavior; asserting on log output here would be low-value test coupling. |
| 447 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:313 | String mutation | The mutation only empties a log message template string in a summary LogInformation call; the returned CacheWarmingResult and all persistence behavior are unchanged, so no test could meaningfully assert on it and it carries no user-facing risk. |
| 448 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:314 | String mutation | The mutation only alters a log message template fragment in a LogInformation summary call; no behavior or returned CacheWarmingResult changes, and tests assert on the result object rather than log text, so this is a low-value logging-string mutation artifact. |
| 449 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:315 | String mutation | The mutation only blanks part of a log message template string in a LogInformation summary call; no behavior or returned CacheWarmingResult changes, and tests would only be able to kill it by asserting on exact log text, which is low-value coupling. |
| 450 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:316 | String mutation | The mutation only alters a log message template string in a LogInformation call; no behavior, return value, or state changes, and the returned CacheWarmingResult is unaffected. Tests legitimately don't assert on log text, so this is a low-value logging mutation artifact. |
| 451 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:319 | Conditional (true) mutation | The mutation only affects the formatting of a log message suffix (an empty 'Failing pairs: .' string instead of nothing) inside a LogInformation call; no observable behavior or returned CacheWarmingResult changes, so it's a low-value logging-only mutation not worth a test. |
| 452 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:319 | Conditional (false) mutation | The mutation only affects the human-readable suffix of an Information-level log message; the returned CacheWarmingResult and all observable behavior are unchanged, so this is a log-formatting-only mutant of no test value. |
| 456 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:319 | String mutation | The mutation only affects the interpolated suffix of a summary log message (Information-level operator diagnostics); the returned CacheWarmingResult and all persisted state are unchanged, so no test could meaningfully assert on it and no user-facing behavior is affected. |
| 454 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:319 | Equality mutation | The mutation only affects the formatting of an Information-level log message suffix (emitting ' Failing pairs: .' instead of empty string when the list is empty); no observable behavior or returned CacheWarmingResult changes, so it's a low-value log-formatting artifact not worth a test. |
| 460 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:332 | Logical mutation | The mutation only changes when an Information-level progress log line is emitted; it has no effect on CacheWarmingResult or any observable behavior, and tests reasonably don't assert on progress-checkpoint logging frequency. |
| 458 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:319 | String mutation | The mutation only alters a log message argument inside a LogInformation call; the returned CacheWarmingResult and all behavior are unchanged, so no test could reasonably or usefully assert on it. |
| 462 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:332 | Equality mutation | The mutated condition only controls whether an Information-level progress log line is emitted; it has no effect on CacheWarmingResult or any observable behavior, and no test asserts on progress-checkpoint logging output. |
| 463 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:332 | Arithmetic mutation | The mutation only alters how often a purely informational progress log line is emitted; no observable behavior, state, or return value changes, and the codebase's tests reasonably do not assert on logging cadence. |
| 461 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:332 | Negate expression | The mutated condition only gates an Information-level progress log line; no observable behavior, return value, or state changes, so tests would only kill it by asserting on log call frequency — a low-value assertion this suite understandably doesn't make. |
| 457 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:319 | String mutation | The mutation only affects the interpolated suffix text inside a LogInformation call; the returned CacheWarmingResult and all persisted state are unchanged, so no observable behavior differs beyond log message content, which tests rightly don't assert. |
| 465 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:333 | Statement mutation | The mutation removes a purely observational Information-level progress log with no effect on CacheWarmingResult or any other behavior; no test asserts on logger output here, and asserting on periodic progress logging would be low-value test surface. |
| 464 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:332 | Equality mutation | The mutation only alters when a purely informational progress log line is emitted; it has no effect on CacheWarmingResult or any observable behavior, and no test asserts on logger output for this checkpoint. |
| 466 | noise | low | backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs:333 | String mutation | The mutation only blanks a log message template string in a progress-checkpoint log call; it has no behavioral effect beyond log text, and no test asserts on log message content (no covering test was found at all). |
