# Code Quality & Health Assessment

**Repository:** `johanpearson/xg-arcade` (single monorepo: `backend/` — C# / ASP.NET Core, `frontend/` — TypeScript / React, `infra/` — Bicep/Azure, `docs/` — governing documentation)
**Assessment date:** 2026-09-13 (previous: 2026-08-23)
**Method:** CodeScene/SonarQube-style manual review — structural discovery, git-churn analysis (`git log --format=format: --name-only | sort | uniq -c | sort -rn`), and targeted deep reads of the largest/most-changed files per component, cross-referenced against this repo's own `docs/architecture-document.md` component boundaries and `docs/coding-guidelines.md`.
**Relationship to `CODEBASE_ANALYSIS.md`:** that document tracks priority-ranked findings (P1-P4) with its own revision history and has driven five completed remediation epics (`docs/backlog.md` Epics 7-9 and 17, plus this revision's Epic 21). This report is a complementary lens — a numeric 1.0-10.0 score per module/component — not a replacement; both were updated together in this sweep, cross-checked against each other and against `docs/backlog.md`'s "Built as" notes before any finding was written up, so genuinely-already-fixed items aren't re-flagged. Sweeps of either kind are owned by the `code-health-auditor` agent (`docs/ai/agent-migration-plan.md` §8).

**Revision history:**
- **2026-08-11** — first assessment. Overall 6.4/10. Three concentrated hotspots below 5.0 (`WikidataClient.cs` 2.5, `GridGameModule.cs` 4.5, frontend 5.5 driven by `LeaderboardScreen.tsx` + no shared fetch hook), plus `architecture-document.md`'s COMP-05/COMP-11 status cells accreting unbounded narrative (one cell 14,718 characters). Findings became `docs/backlog.md` Epic 9 (S-115–S-129).
- **2026-08-18 (this revision)** — re-scored every module/component from scratch after verifying Epic 9's actual completion state against `git log`/`docs/backlog.md` (11 of Epic 9's 15 stories had shipped since 2026-08-11 but several were missing their "Built as" note — added in this pass, not re-implemented). **Overall system score: 7.6/10 (+1.2)**. All three 2026-08-11 hotspots substantially resolved: `GridGameModule.cs` split (ADR-0068) → 8.2/10; `WikidataClient.cs` deduplicated (S-118 + this sweep's S-124 fix) → 5.5/10 (still one large multi-concern file, but the defect-risk-driving duplication is gone); frontend's `LeaderboardScreen.tsx` split + `useAuthedFetch` hook shipped → 7.3/10. `architecture-document.md`'s accretion fixed (S-116/S-123, verified: largest table cell now 1,964 characters, down from 14,718). Two stale citations Epic 9 had explicitly deferred (§6.2c's ADR-0052→ADR-0053, §6.10's `XGArcade.Testing`→`Testing.SeedManager` naming, plus one adjacent stale "not yet built" claim found alongside) were fixed directly in this sweep, not carried forward. New hotspot-in-progress identified: `XGPathGameModule.cs` grew from 423→557 lines (+32%, 8 commits since 2026-08-11) without the restructuring the 2026-08-11 revision flagged as a "pre-emptive refactor candidate" — now the clearest next-round target, tracked as `docs/backlog.md` Epic 17 S-154. Full findings and new epic below.
- **2026-08-22 (previous revision)** — verified Epic 17's actual completion state against `git log`/current code first (all 5 stories, S-154–S-158, confirmed shipped — one doc-drift found and fixed: S-154's `docs/backlog.md` entry was missing its "Built as" note despite the extraction/ADR-0082/doc-sync having genuinely landed). **Overall system score: 8.1/10 (+0.5)** — the first revision to cross this report's own 8.0 target on the whole-system average; individual outliers below it remain (see below). `XGArcade.Games.XGPath` → 8.1/10 (+1.8 since 2026-08-18): `XGPathGameModule.cs` split into a 291-line thin adapter + `PathEligibilityService.cs` (363 lines) per ADR-0082, mirroring `GridGameModule`'s ADR-0068 split exactly. `XGArcade.DataSync` → 7.5/10 project-level (+0.8 since 2026-08-18; `WikidataClient.cs` itself 8.0/10, +2.5): S-155 split `SparqlQueryBuilders.cs`/`SparqlResponseParsers.cs` out, taking `WikidataClient.cs` from 1,775→782 lines — its remaining public surface is now ~15 thin wrapper methods over two shared drivers, no longer a breadth concern. `frontend/` → 8.3/10 (+1.0): `AdminScreen.tsx`'s last 4 subcomponents gained dedicated tests (S-156), `AdminScreen.tsx` itself migrated onto `useAuthedFetch` (S-157), and `App.tsx`'s auth-session lifecycle extracted into `useSession()` (S-158, 649→529 lines) — closing every outstanding Epic 17 frontend item. One new hotspot-in-progress identified, replacing the now-resolved xG Path finding: `PlayerCareerPrefetchService.cs` (`XGArcade.DataSync`, 6.5/10) — its country/club sweep loops have grown near-identically duplicated across 8 commits since 2026-08-11, the same "duplicated block" pattern this report has already fixed twice elsewhere (`WikidataClient.cs`'s HTTP handling, `GridGameModule.cs`'s methods). Tracked as `docs/backlog.md` Epic 21 S-165. All frontend tooling ran live and clean this pass: 647/647 `npm run test` (44 files, up from 584/584), `tsc -b` clean, `oxlint` clean, fresh `npm install`. `npm audit` unchanged from 2026-08-18 (`nanoid@<3.3.18`, dev-only, Dependabot's lane). No `dotnet` SDK available, confirmed again.
- **2026-08-23 (this revision)** — a deliberately WIDER pass than 2026-08-22's, at the requester's explicit direction: instead of stopping at the single highest-scoring-gap finding, every module still below ~9.0 in the 2026-08-22 revision was re-read, and backend/frontend/infra were each searched past the #1 hotspot for the same duplicated-block/god-file/weak-coverage/boundary-smell patterns this lineage has already caught before. S-165 (`PlayerCareerPrefetchService.cs`) is unchanged and still open — not re-described here, still `docs/backlog.md` Epic 21. **Overall system score: 7.9/10 (-0.2)** — this movement is newly-*counted* pre-existing complexity, not a regression: no code changed this pass (findings/planning, not remediation; see `docs/backlog.md` Epic 22 for the four new stories filed), it simply looked harder in three modules (`XGArcade.Api`, `XGArcade.Games.XGGrid`, `frontend/`) that hadn't had a below-the-top-hotspot pass before. Four new findings, all the same "duplicated shape repeated per near-identical case" pattern this lineage keeps catching at different granularities: **`PlayerCacheWarmingService.cs`** (`XGArcade.Games.XGGrid`, new — 6.5/10) duplicates the *exact same* Country×Club/Club×Club 5-branch sweep shape `PlayerCareerPrefetchService.cs` (S-165) duplicates one directory over — the third occurrence of this specific shape in the codebase. **`CliVerbDispatcher.cs`** (`XGArcade.Api`, still 8.1/10 at the verb-registry level per S-112/S-114, but a narrower internal finding at a finer grain: 5 of its 14 handlers repeat the same Wikidata-`HttpClient` bootstrap inline) — the single highest-churn file in the repo (13 commits), re-read at a level of detail the 2026-08-18/2026-08-22 revisions' "verb-registry pattern holding" verdict didn't reach. **`frontend/src/lib/*.ts`** (new component-level row, 7.5/10) — 47 `fetch`+`throwApiError`+`json()` call sites duplicated across 8 domain files (`admin.ts`/`auth.ts`/`announcements.ts`/`leaderboard.ts`/`rounds.ts`/`leagues.ts`/`incidents.ts`/`path.ts`), never visible as a single-file outlier before because no one file was large enough to flag on its own — only visible aggregated. **`GridScreen.tsx`/`PathScreen.tsx`** (`frontend/`, still 8.3/10 project-level) duplicate their own `LoadState` machine, round-fetch effect, autocomplete warm-up effect, and `handleViewCompletedRoundLeaderboard` (the latter's own comment states it "mirrors GridScreen.tsx... exactly") — previously out of scope because Epic 17's frontend pass was `AdminScreen.tsx`/`App.tsx`-scoped, and these two screens' own `useAuthedFetch.ts` evaluation asked a different question (mutable post-fetch state) than "do these two screens duplicate each other." All four tracked as `docs/backlog.md` Epic 22 (S-166–S-169). `npm run test` (647/647, 44 files), `tsc -b`, and `oxlint` all re-ran live and clean this pass (existing `node_modules/`, no reinstall needed); `npm audit` unchanged (`nanoid@<3.3.18`, dev-only). No `dotnet` SDK available, confirmed again — no backend code was changed this pass (findings-only), consistent with that constraint.
- **2026-09-13 (this revision)** — requested explicitly weighted toward code that landed since 2026-08-23, with a 9.0/10 target. **Step 0 first:** this repository's own `git log` only reaches back to 2026-09-02 (a repository-history quirk, not a doc-accuracy problem — every commit SHA Epics 22-24's "Built as" notes cite predates that boundary and isn't resolvable via `git log` here), so Epic 22's completion state was verified by reading current code directly instead: `PlayerCacheWarmingService.cs`'s `SweepPairsAsync`, `PlayerCareerPrefetchService.cs`'s `SweepAsync`, `CliVerbDispatcher.cs`'s `BuildWikidataClient` (now called from **8** handlers, up from S-167's original 5 — new call sites correctly reuse it, not re-inlining the bootstrap), `frontend/src/lib/apiClient.ts`'s `apiRequest<T>`, and `frontend/src/lib/useRoundFetch.ts` all confirmed present and matching their stories' text — **Epics 22, 23, and 24 (S-165–S-176) are fully shipped**, none re-done. Since then, three entire new games landed (xG Predict, xG Connect, xG Higher/Lower — `XGArcade.Games.XGPredict`/`XGConnect`/`XGHigherLower`), plus REQ-1420 (dispute-visibility gate), S-234/ADR-0114 (round-generation renewal), avatar uploads, friends/challenges/matchmaking, and Supabase egress remediation. **Overall system score: 8.3/10 (+0.4)** — Epics 22-24's fixes holding under real subsequent growth (`WikidataClient.cs` grew 782→1,028 lines and `CliVerbDispatcher.cs` 769→1,037, both purely via new thin call sites over their existing shared helpers, confirmed by reading every new method — not a regression) plus a full sweep of the three new games and frontend feature areas, most of which are genuinely healthy: `ServiceRegistration.cs` (761 lines, 18 commits, now the single highest-churn source file in the repo) re-confirmed a cohesive one-line-per-service registry, not a hotspot; `frontend/src/social/*` proactively shares `FetchListSection.tsx` rather than duplicating per-tab fetch logic — ADR-0084's per-diff code-health budget visibly working. **One new hotspot-in-progress:** `App.tsx` regrew from 529 lines (post-S-158) to **826 lines** across five new top-level screens, each individually well-reasoned but collectively re-forming the "duplicated near-identical case" pattern one level up (four separate seed-then-navigate handlers, the hash-routing mechanism still un-extracted) — tracked as `docs/backlog.md` Epic 30 S-235. **One doc-consistency defect, not a code finding:** `docs/backlog.md` has two colliding `## Epic 13` headers (a genuine numbering slip, confirmed via its own `docs/CHANGELOG.md` 2026-08-31 entry saying "new Epic 13") with ~15 ambiguous cross-references elsewhere — too risky to disambiguate solo in one pass without misreading a reference, tracked as Epic 30 S-236 for `doc-sync`. `npm install` (fresh), `npm run test -- --run` (993/993 across 74 files, up from 647/647), `tsc -b`, and `oxlint` all ran live and clean (oxlint: 3 pre-existing, deliberate `only-export-components` warnings in `PlayerRefreshFieldsList.tsx`, inspected and not a finding). `npm audit`: 3 vulnerabilities (up from 1) — `nanoid@<3.3.18` (high, unchanged) plus a new `@vitest/mocker` path-traversal advisory (moderate) — both dev-only transitive, Dependabot's lane. No `dotnet` SDK, confirmed again; S-234's own `ci.yml workflow_dispatch` verification run is still recorded pending as of 2026-09-11 — flagged for the orchestrating session, which holds the GitHub Actions tool this sweep doesn't.

---

## 1. Executive Summary

- **Overall System Code Health Score: 8.3 / 10 (+0.4) — above this report's own 8.0 whole-system target; still short of the 9.0 target set for this pass — see §4 for the story that would close most of the remaining gap**
- **Key Takeaway:** this pass verified Epics 22-24 (S-165–S-176) are genuinely fully shipped by reading current code directly (this repo's own `git log` only reaches back to 2026-09-02, so the SHAs those stories' "Built as" notes cite aren't independently resolvable here — a history quirk, not a documentation gap), then weighted the rest of the sweep toward everything that landed since: three entire new games (xG Predict, xG Connect, xG Higher/Lower), REQ-1420's dispute-visibility gate, S-234/ADR-0114's round-generation renewal, avatar uploads, and friends/challenges/matchmaking. The headline result is that Epics 22-24's fixes are **holding under real subsequent load, not just present** — `WikidataClient.cs` and `CliVerbDispatcher.cs` both grew substantially again (1,028 and 1,037 lines respectively) but every new addition correctly reuses the shared helpers those epics extracted, confirmed by reading every new method rather than trusting the line-count delta alone. One new hotspot-in-progress surfaced: `App.tsx` regrew from 529 to 826 lines across five new top-level screens, reforming the same "duplicated near-identical case" pattern one level up in its own seed-and-navigate handlers — tracked as `docs/backlog.md` Epic 30 S-235, the clearest single story left to close before 9.0. One doc-consistency defect (not a code finding) also surfaced: `docs/backlog.md` has two colliding `## Epic 13` headers from a genuine historical numbering slip, with ~15 ambiguous cross-references elsewhere — too risky to fix solo without misreading a reference, tracked as S-236 for `doc-sync`. Frontend tooling all ran live and clean this pass (993/993 tests across 74 files, up from 647/647 — substantial new coverage for the three new games and friends/challenges; `tsc -b` and `oxlint` clean; `npm audit` up to 3 dev-only advisories, Dependabot's lane). No `dotnet` SDK in this sandbox, confirmed again — nothing backend-touching was changed directly this pass; S-234's own CI-verification run is still recorded pending, flagged for the orchestrating session.

---

## 2. Score Breakdown by Repository / Module

*(Single monorepo — rows are the natural sub-project boundaries this codebase already uses: `backend/src/*`, `frontend/`, `infra/`, `docs/`.)*

| Module | Code Health Score (1-10) | Δ since 2026-08-23 | Risk Level | Primary Health Drivers / Issues |
|---|---|---|---|---|
| `XGArcade.Core` | 8.4 / 10 | +0.1 | Low | New `XGPredictScoringStrategy`/`HigherLowerScoringStrategy` follow the same thin `IScoringStrategy` shape `UniquenessScoringStrategy`/`ClueEfficiencyScoringStrategy` already established — no new concern. ADR-0003 boundary re-verified intact across all five games now hosted, not just the two that existed at the last check. |
| `XGArcade.Data` | 8.4 / 10 | — | Low | Unchanged. ADR-0067's repository split still holding — every new entity this era (`ConnectMatch`, `Friend`, `Challenge`, `MatchmakingOptIn`, `AvatarSubmission`, `HigherLowerAttempt`) got its own narrow repository, no facade. Migration-generated code still excluded from scoring. |
| `XGArcade.Api` | 8.3 / 10 | +0.4 | Low | S-167's `BuildWikidataClient` fix (Epic 22) confirmed landed and holding under growth: `CliVerbDispatcher.cs` is now 1,037 lines (up from 769) but the shared helper is called from **8** handlers (up from 5), every new one reusing it correctly. `ServiceRegistration.cs` (761 lines, 18 commits — now the single highest-churn source file in the repo) read in full: still one line/block per registered service with its own doc comment, zero duplicated registration shape — large but cohesive, not a hotspot, same verdict as Epic 22's own `WikidataLookupService.cs`/`ServiceRegistration.cs` watch-only findings. `AuthController.cs` unchanged. |
| `XGArcade.DataSync` | 8.2 / 10 | +0.7 | Low-Medium | S-165's `SweepAsync` fix (`PlayerCareerPrefetchService.cs`) and S-172's doc fix both confirmed landed. `WikidataClient.cs` grew 782→1,028 lines (new trophy-stats/international-stats/sitelink-count queries for xG Higher/Lower) but every added method is a thin wrapper over the existing shared `RunIntersectionQueryAsync`/`RunThrowingQueryAsync` drivers, confirmed by reading each one — breadth without duplication. |
| `XGArcade.Games.XGGrid` | 8.3 / 10 | +0.7 | Low | S-166's `SweepPairsAsync` fix confirmed landed (`PlayerCacheWarmingService.cs` 388→367 lines) and S-170's unused-`ILogger` cleanup confirmed landed. `GridGameModule.cs` unchanged, still a healthy thin adapter (ADR-0068). |
| `XGArcade.Games.XGPath` | 8.1 / 10 | — | Low | Unchanged. `PathCareerStintFilter.cs` still cohesive, watch-only. |
| `XGArcade.Games.XGPredict` | 7.8 / 10 (new row) | n/a | Low-Medium | New module (REQ-1301-1306). `XGPredictGameModule.cs` (294 lines) is the one remaining `IGameModule` implementation without a Grid/Path-style generation-service split — read in full, still cohesive (nine methods, one real algorithm), well below the ~600-line size that triggered those earlier splits. Watch-only, not a story; see `docs/backlog.md` Epic 30's "investigated and declined" list. |
| `XGArcade.Games.XGConnect` | 7.9 / 10 (new row) | n/a | Medium | New module (REQ-1401-1415, ADR-0103/ADR-0109), 12 files each 27-297 lines, one focused service per concern (target-pick, chain-step, dispute, chat, lifecycle, query), no duplication found. Scored slightly conservative relative to its own cleanliness because it's the newest, most actively-churning module (REQ-1420's dispute-visibility gate just landed) — revisit at the next sweep once churn settles. |
| `XGArcade.Games.XGHigherLower` | 8.0 / 10 (new row) | n/a | Low | New module (REQ-1501-1508, ADR-0110-0113). Notably, its 2026-09-10 "delegation-pattern refactor" (`HigherLowerGenerationService` split out of `XGHigherLowerGameModule`) proactively applied the exact lesson `GridGameModule`'s own ADR-0068 split learned the hard way — a good sign this lineage's fixes are generalizing, not just being re-applied reactively by this sweep. |
| `frontend/` | 8.0 / 10 (project) — **`App.tsx` 7.0 / 10 (new finding), rest 8.2-8.4** | +0.1 | Medium (1 new driver) | S-168/S-169 (`apiClient.ts`'s `apiRequest<T>`, `useRoundFetch.ts`) both confirmed landed and holding — `GridScreen.tsx`/`PathScreen.tsx` now share the hook as designed. New finding: `App.tsx` regrew from 529 lines (post-S-158) to 826 across five new top-level screens (xG Predict/Connect/Higher-Lower, Friends, Stats) — each addition individually well-reasoned and consistent with the file's own established pattern, but the aggregate reforms the "duplicated near-identical case" pattern one level up (four separate seed-then-navigate handlers, the hash-routing mechanism still un-extracted). Tracked as `docs/backlog.md` Epic 30 S-235. Elsewhere, `frontend/src/social/*` (new, for REQ-1401-1403) proactively shares `FetchListSection.tsx` rather than duplicating per-tab fetch logic — ADR-0084's per-diff code-health budget visibly working. All tooling ran live and clean: 993/993 `npm run test` (74 files, up from 647/647), `tsc -b` clean, `oxlint` clean (3 pre-existing, deliberate warnings in `PlayerRefreshFieldsList.tsx`, not a finding). `npm audit`: 3 dev-only advisories (up from 1), Dependabot's lane. |
| `infra/` (Bicep + scripts) | 8.4 / 10 | +0.4 | Low | S-173 (`main.parameters.json` deleted), S-174 (`validate-bicep.yml` CI check), S-175/S-176 (composite actions `run-cli-verb`/`trigger-round-generation`) all confirmed landed on disk. The `run-cli-verb` composite action now backs 12+ workflows (up from S-175's original 7, new verbs added since all correctly reusing it) — the fix scaled cleanly under new-game growth. `sync-prod-to-dev.sh`/`promote-dev-to-prod.sh` unchanged, still watch-only. |
| `docs/` (governing documents) | 7.9 / 10 | -0.1 | Low | `architecture-document.md`'s COMP-07 stale-claim fix (S-172) confirmed landed. One new, concrete doc-consistency defect (not accretion): `docs/backlog.md` has two colliding `## Epic 13` headers (a genuine 2026-08-31 numbering slip, confirmed via its own `docs/CHANGELOG.md` entry), with ~15 ambiguous cross-references elsewhere — tracked as Epic 30 S-236 for `doc-sync` rather than fixed solo, since misreading even one reference while renumbering would corrupt it. `CODE_HEALTH_ASSESSMENT.md`/`CODEBASE_ANALYSIS.md` themselves were stale relative to Epics 22-29 before this revision — refreshed as part of this pass. No accretion re-found (`design-document.md`'s dense cells still judged proportionate, unchanged verdict). |

---

## 3. Score Breakdown by Component / Layer

*(Mapped to this repo's own C4/COMP-xx architecture components, per `docs/architecture-document.md` §5.)*

| Component / Layer | Location | Score (1-10) | Δ | Key Metrics / Smells Observed |
|---|---|---|---|---|
| Core.Scoring / Core.Rounds / Core.Users (COMP-01/03/04) | `backend/src/XGArcade.Core` | 8.4 / 10 | +0.1 | New `XGPredictScoringStrategy`/`HigherLowerScoringStrategy` follow the established thin-strategy shape. `RoundGenerationService.cs` (210 lines) re-read after S-234/ADR-0114's renewal-branch addition — cohesive, correctly routed through `IRoundScoreSourceResolver` per that story's own architecture-review fix, not a direct repository call. |
| Data.PlayerStore (COMP-06) | `backend/src/XGArcade.Data/Repositories` | 8.4 / 10 | — | Unchanged. Post-ADR-0067 split still holding. |
| DataSync.Clients (COMP-07) | `backend/src/XGArcade.DataSync/Wikidata/WikidataClient.cs` | 8.0 / 10 | — | Grew 782→1,028 lines (new trophy-stats/international-stats/sitelink-count queries backing xG Higher/Lower) — every added method confirmed a thin wrapper over the existing `RunIntersectionQueryAsync`/`RunThrowingQueryAsync` drivers, same shape S-155/S-172 already described. Not re-flagged. |
| DataSync.PlayerCareerPrefetch (COMP-07) | `backend/src/XGArcade.DataSync/Wikidata/PlayerCareerPrefetchService.cs` | 8.0 / 10 | +1.5 | **Resolved (S-165), confirmed landed by reading current code.** The country/club sweep loops now both call a shared `SweepAsync` helper (`SweepClubsAsync`/`SweepCountryClubPairsAsync`-style callers), verified by direct grep — no remaining duplication. |
| Games.XGGrid.PlayerCacheWarming (COMP-05) | `backend/src/XGArcade.Games.XGGrid/PlayerCacheWarmingService.cs` | 8.2 / 10 | +1.7 | **Resolved (S-166), confirmed landed.** 367 lines (down from 388), `SweepCountryClubPairsAsync`/`SweepClubClubPairsAsync` now both thin wrappers over a shared private generic `SweepPairsAsync<TLeft, TRight>` — verified by direct grep, no remaining duplication. |
| Frontend.LibFetch (CONT-01) | `frontend/src/lib/apiClient.ts` + 8 domain files | 8.3 / 10 | +0.8 | **Resolved (S-168), confirmed landed.** `apiClient.ts` (102 lines) now exports `apiRequest<T>`, used across the domain files; all 47 call sites' status-code special-casing (404-as-data, etc.) preserved per the story's own note. |
| Frontend.RoundFetchDuplication (CONT-01) | `frontend/src/grid/GridScreen.tsx`, `frontend/src/path/PathScreen.tsx`, `frontend/src/lib/useRoundFetch.ts` | 8.2 / 10 | +0.4 | **Resolved (S-169), confirmed landed.** New `useRoundFetch.ts` (138 lines) exports `useRoundFetch`/`useAutocompleteWarmup`, both screens now call it. |
| Frontend.AppNavigation (CONT-01, new row) | `frontend/src/App.tsx` | 7.0 / 10 (new finding) | n/a | 826 lines (up from 529 post-S-158), 8 commits. Five new top-level screens each added their own "seed a target, then `navigateTo`" handler (`handleViewRoundLeaderboard`, `handleOpenOwnStats`/`handleSelectPlayerStats`, `handleOpenFriendsTab`) plus a `SCREEN_HASHES`/`HASH_TO_SCREEN` entry — every individual addition well-reasoned and consistent with the file's own established pattern, but the aggregate reforms the same duplicated-near-identical-case shape this lineage has caught seven times before, one level up from where `useSession` (S-158) already extracted the auth-lifecycle half. Tracked as `docs/backlog.md` Epic 30 S-235. |
| Games.XGGrid (COMP-05) | `backend/src/XGArcade.Games.XGGrid/GridGameModule.cs` | 8.2 / 10 | — | Unchanged. S-170's unused-`ILogger` cleanup confirmed landed (no `logger` parameter in `GridGameModule.cs`/`GridLiveLookupDispatcher.cs`). |
| Games.XGPath (COMP-11) | `backend/src/XGArcade.Games.XGPath/` | 8.1 / 10 | — | Unchanged. |
| Games.XGPredict (COMP-15, new row) | `backend/src/XGArcade.Games.XGPredict/` | 7.8 / 10 | n/a | New module (REQ-1301-1306, ADR-0095-0102). `XGPredictGameModule.cs` (294 lines) is the one `IGameModule` without a Grid/Path-style split — cohesive today, watch-only; revisit past ~450 lines or a second concern. |
| Games.XGConnect (COMP-17, new row) | `backend/src/XGArcade.Games.XGConnect/` | 7.9 / 10 | n/a | New module (REQ-1401-1415, ADR-0103/ADR-0109). 12 files, one focused service per concern (`ConnectTargetPickService`/`ConnectChainStepService`/`ConnectChainStepDisputeService`/`ConnectChatService`/`ConnectMatchLifecycleService`/`ConnectMatchQueryService`), largest 297 lines. No duplication found; scored conservatively as the newest, still-actively-churning module (REQ-1420 just landed). |
| Games.XGHigherLower (COMP-18, new row) | `backend/src/XGArcade.Games.XGHigherLower/` | 8.0 / 10 | n/a | New module (REQ-1501-1508, ADR-0110-0113). Its own 2026-09-10 delegation-pattern refactor (`HigherLowerGenerationService` split out of the game module) proactively mirrors `GridGameModule`'s ADR-0068 split — evidence this lineage's fix pattern is generalizing to new code, not just reactively re-applied by this sweep. |
| API / Routing (CONT-02) | `backend/src/XGArcade.Api` | 8.3 / 10 | +0.4 | S-167's `BuildWikidataClient` confirmed landed and now used by 8 handlers (up from 5). `CliVerbDispatcher.cs` (1,037 lines, still the single highest-churn backend file, now 1,037 lines) and `ServiceRegistration.cs` (761 lines, 18 commits, now the single highest-churn *source* file in the repo overall) both re-read in full this pass — both are large registries growing one clean entry per new feature/game, no duplication in either. `AuthController.cs` unchanged. |
| Web Frontend (CONT-01) | `frontend/src` | 8.0 / 10 | +0.1 | S-168/S-169 confirmed landed and holding — see the dedicated rows above. New finding scoped to `App.tsx` (see Frontend.AppNavigation row above). `frontend/src/social/*` (new) proactively shares `FetchListSection.tsx` rather than duplicating per-tab fetch logic. |
| Infrastructure as Code | `infra/bicep`, `infra/scripts`, `.github/workflows`, `.github/actions` | 8.4 / 10 | +0.4 | S-173/S-174/S-175/S-176 all confirmed landed: `main.parameters.json` deleted, `validate-bicep.yml` added, `.github/actions/run-cli-verb`/`trigger-round-generation` composite actions extracted and now backing 12+ workflows (grown from the original 7, every new verb correctly reusing them). `sync-prod-to-dev.sh`/`promote-dev-to-prod.sh` unchanged, still watch-only. |
| Documentation / Process | `docs/`, `MVP-SCOPE.md`, `CLAUDE.md` | 7.9 / 10 | -0.1 | S-172's COMP-07 doc fix confirmed landed. New, concrete finding (not accretion): `docs/backlog.md` has two colliding `## Epic 13` headers from a genuine 2026-08-31 numbering slip, ~15 ambiguous cross-references elsewhere — tracked as Epic 30 S-236. `CODE_HEALTH_ASSESSMENT.md`/`CODEBASE_ANALYSIS.md` themselves refreshed as part of this pass (were stale relative to Epics 22-29 beforehand). No new accretion found elsewhere. This sandbox still has no `dotnet` SDK, confirmed again. |

---

## 4. Priority Refactoring Targets

### 🟠 Hotspot-in-Progress (Medium-High Complexity, Rising Churn)

1. **`frontend/src/App.tsx`** (826 lines, 8 commits since S-158's 529-line extraction) — new this pass, the clearest single remaining gap to the 9.0 target. Four separate "seed a target, then `navigateTo`" handlers plus the `SCREEN_HASHES`/`HASH_TO_SCREEN`/`screenForHash` hash-routing mechanism, all still living directly in `App`'s own function body — the same duplicated-shape pattern this report has now caught eight times, one level up from where `useSession` (S-158) already extracted the auth-lifecycle half of this same file. **Fix:** extract a `useAppNavigation`-shaped hook — see `docs/backlog.md` Epic 30 S-235.

Every prior hotspot this report has tracked (`PlayerCareerPrefetchService.cs`, `PlayerCacheWarmingService.cs`, `CliVerbDispatcher.cs`'s Wikidata-client bootstrap, `frontend/src/lib/*.ts`, `GridScreen.tsx`/`PathScreen.tsx`) is **resolved and confirmed still holding this pass** — see §3 above for each. No backend hotspot-in-progress remains open.

### 🟡 Large-but-Healthy (Watch, Not Urgent)

1. **`backend/src/XGArcade.Games.XGPath/PathCareerStintFilter.cs`** (544 lines) — re-confirmed cohesive again this pass.
2. **`backend/tests/XGArcade.DataSync.Tests/Wikidata/WikidataClientTests.cs`** (3,973 lines, still the single largest file in the repo including tests) — unchanged, growing for legitimate regression-proof reasons.
3. **`infra/scripts/sync-prod-to-dev.sh`/`promote-dev-to-prod.sh`** (83/85 lines) — unchanged, still a deliberate safety-property divergence, not a finding.
4. **`backend/src/XGArcade.Api/CompositionRoot/ServiceRegistration.cs`** (761 lines, 18 commits — now the single highest-churn source file in the repo) and **`CliVerbDispatcher.cs`** (1,037 lines) — both re-read in full this pass. Both are large registries that grow one clean entry per new feature/game, with zero duplicated shape in either. Watch, not urgent — re-verify at the next sweep that new entries keep following the established pattern rather than drifting.
5. **`backend/src/XGArcade.Games.XGPredict/XGPredictGameModule.cs`** (294 lines, new) — the one `IGameModule` without a generation-service split, still well below the size that triggered `GridGameModule`'s/`XGPathGameModule`'s own splits. Revisit past ~450 lines.

### 🟢 Quick Wins (Low Effort / High ROI)

None applied directly this pass — `App.tsx`'s extraction (S-235) is frontend-verifiable in a normal session (no `dotnet` needed) but large enough (826 lines, a live routing component with heavy existing test coverage in `App.test.tsx`'s 1,918 lines) to warrant its own reviewed session rather than a same-session mechanical edit, consistent with this report's own bar for what's safe to fix solo. `docs/backlog.md`'s duplicate `## Epic 13` header (S-236) is a doc-only fix but spans ~15 cross-references across 4 other files — same reasoning, filed rather than fixed solo.

### 🏗️ Architectural & Structural Debt

- **No boundary violations found.** ADR-0003 re-verified intact across all five games now hosted (not just the two that existed at the last check) by direct grep of cross-namespace references — only doc-comment mentions of a `GameKey` constant, no real type/namespace reference from `XGArcade.Core`/`XGArcade.Data.Entities` into any `XGArcade.Games.*` project.
- **The "duplicated shape repeated per near-identical block" pattern this report has now caught and fixed nine times** across four sweeps (`WikidataClient.cs`, `GridGameModule.cs`, `XGPathGameModule.cs`, `PlayerCareerPrefetchService.cs`, `PlayerCacheWarmingService.cs`, `CliVerbDispatcher.cs`'s bootstrap, `frontend/src/lib/*.ts`, `GridScreen.tsx`/`PathScreen.tsx`) **has a tenth instance this pass, back on the frontend** (`App.tsx`'s seed-and-navigate handlers) — the same shape recurring at a different granularity than either of the two frontend instances Epic 22 already caught. Notably, this pass also found the pattern being caught *proactively* for the first time (`frontend/src/social/*`'s shared `FetchListSection.tsx`, xG Higher/Lower's own delegation-pattern split) — evidence ADR-0084's per-diff code-health budget is starting to catch instances before they need a dedicated sweep story.
- **Verification debt, still systemic:** this sandbox has no `dotnet` SDK, confirmed again. Frontend tooling is fully available and ran live and clean (993/993 tests across 74 files, `tsc -b`, `oxlint`). S-234's own `ci.yml workflow_dispatch` verification run (REQ-305/ADR-0114) is recorded pending as of 2026-09-11 in `docs/backlog.md` — this sweep found no evidence it has since run; flagged for the orchestrating session, which holds the GitHub Actions tool this sub-agent doesn't.
